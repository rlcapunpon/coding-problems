Ramon's pyscripts

Split the file:

python split_file.py <input_path> <output_directory> <line_count>

e.g. python split_file.py FM_NBI_logs.txt 1000


Find the files with error:

python find_error.py input_directory output_directory word_to_search

e.g. python find_error.py logsFolder/ errorFolder/ ERROR